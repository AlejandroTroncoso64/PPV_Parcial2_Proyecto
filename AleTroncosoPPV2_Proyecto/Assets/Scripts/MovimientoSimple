using UnityEngine;

public class MovimientoSimple : MonoBehaviour
{

    /* Cambios:
    - Reemplazo de MonoAdvance por MonoBehaviour, MonoAdvance no se si existe en Unity/C# para empezar
    - Variable velocidad modificada a float, y no int, se necesitan también los decimales para calcular movimiento
    - El "new Vector" no tenía el número 3 para designar en qué dimensiones se va a mover.
    - En el paréntesis del new Vector el movimiento está definido en X (Horizontal) y en Z (Vertical), pero el movimiento vertical es en Y, no Z. Se modificó el orden para que las variables de movimiento horizontal y vertical estuvieran respectivos a como se acomodan en el paréntesis, X, Y y Z. Se pone el 0f al final para aclarar que en Z no habrá movimiento.
    - En transform.Transalte se reemplazaron las llaves por paréntesis, las llaves son para definir donde empieza y termina el contenido de una clase, función, etc.
    - Faltaba añadir una mayúscula a time.DeltaTime, en el primer Time para ser exactos.
    */
    public float velocidad = 5f;

    void Update()
    {
        // Obtener la entrada del teclado
        float movimientoHorizontal = Input.GetAxis("Horizontal");
        float movimientoVertical = Input.GetAxis("Vertical");

        // Calcular el vector de movimiento
        Vector3 movimiento = new Vector3(movimientoHorizontal, movimientoVertical, 0f);

        // Mover el objeto en la dirección calculada
        transform.Translate(movimiento * velocidad * Time.deltaTime, Space.World);
    }
}
